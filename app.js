let tonconnect; document.getElementById('connectWallet').onclick = async () => { tonconnect = new TonConnect(); try{ await tonconnect.connect(); alert("Wallet connected: " + tonconnect.account.address); }catch(e){ alert("Connection cancelled"); } }; document.getElementById('amountIn').addEventListener('input', (e)=>{ const ton = parseFloat(e.target.value || 0); document.getElementById('amountOut').value = ton * 100; }); document.getElementById('buyToken').onclick = ()=>{ if(!tonconnect){ alert("Connect wallet first"); return; } const amount = document.getElementById("amountIn").value; alert("Simulation: " + amount + " TON → " + (amount*100) + " SYN"); };
